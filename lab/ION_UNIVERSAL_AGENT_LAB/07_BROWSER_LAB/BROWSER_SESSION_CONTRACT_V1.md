# BROWSER SESSION CONTRACT V1

The lab may use a dedicated non-production candidate session or a governed read-only attachment to the existing single browser owner. It must never create a competing production browser owner.

Persist: session id, target URL, viewport, language, currency, shop, role, active product context and evidence refs.

Mutating browser actions outside lab candidates require an explicit gate.
