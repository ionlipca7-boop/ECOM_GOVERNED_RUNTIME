# ROLLBACK CONTRACT V1

Every proposed production integration must include exact target paths, pre-SHA, candidate SHA, dependency list, tests, reversible procedure and proof that no unrelated owner is modified.

Integration is blocked when pre-SHA drifts, rollback is absent, or browser acceptance is incomplete.
